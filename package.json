{
  "name": "booking-calendar-spec",
  "version": "1.0.0",
  "private": true,
  "description": "API-контракт сервиса бронирования слотов (владелец календаря + гости)",
  "type": "module",
  "scripts": {
    "build": "tsp compile spec",
    "watch": "tsp compile spec --watch",
    "format": "tsp format \"spec/**/*.tsp\""
  },
  "dependencies": {
    "@typespec/compiler": "^1.0.0",
    "@typespec/http": "^1.0.0",
    "@typespec/openapi": "^1.0.0",
    "@typespec/openapi3": "^1.0.0",
    "@typespec/rest": "^0.70.0"
  }
}
